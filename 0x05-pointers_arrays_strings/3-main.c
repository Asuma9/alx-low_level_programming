#include "main.h"
#include <stdio.h>
/**
 * main - fucntion that prints a string, followed by a new line to stdout
 * Return: 0 always
 */

int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
