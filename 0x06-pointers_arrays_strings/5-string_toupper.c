#include "main.h"
/**
 * *string_toupper - function to change all lowercase letter to uppercase
 * @s: string pointer
 * Return: void
 */
char *string_toupper(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		if (s[i] >= 'a' && s[i] <= 'z')
		{
			s[i] = s[i] - 32;
		}
		i++;
	}
	return (s);
}
